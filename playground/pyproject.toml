[project]
name = "pokersim-playground"
version = "0.1.0"
description = "Interactive poker playground to play against trained AI models"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0.0",
    "torch>=2.0.0",
    "numpy>=1.24.0",
]
